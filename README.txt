1) This exercise uses Maven. It can be downloaded from https://maven.apache.org/

2) The application can be run from the test-exercise directory using the following command:

   mvn spring-boot:run
   
3) The test suite can be run using the follwoing command:

   mvn test
   
4) The API is accessible from http://localhost:8080/reporting

